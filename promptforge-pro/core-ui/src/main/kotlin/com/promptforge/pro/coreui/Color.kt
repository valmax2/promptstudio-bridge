package com.promptforge.pro.coreui

import androidx.compose.ui.graphics.Color

/**
 * Palette del tema dark premium (vedi master prompt §11):
 * - viola: colore primario dell'app
 * - verde: identità/soggetto (Director Map, consistenza personaggio)
 * - arancione: camera e movimento (Director Map)
 * - rosa: modalità adulti
 */
object PromptForgeColors {
    val Violet = Color(0xFF8B5CF6)
    val VioletContainer = Color(0xFF2E1B57)
    val Green = Color(0xFF34D399)
    val Orange = Color(0xFFFB923C)
    val Pink = Color(0xFFF472B6)

    val Background = Color(0xFF0F0F14)
    val Surface = Color(0xFF17171F)
    val SurfaceVariant = Color(0xFF232330)
    val OnSurface = Color(0xFFEDEDF2)
    val OnSurfaceMuted = Color(0xFFA1A1AE)
    val Border = Color(0xFF2E2E3A)
    val Error = Color(0xFFF87171)
}
