# Regole ProGuard/R8 specifiche di PromptForge Pro vanno qui man mano che servono
# (es. per Room, Hilt o serializzazione, se le regole di default dei rispettivi
# plugin non bastassero). Vuoto per ora: nessun modulo genera ancora codice che
# ne ha bisogno.
