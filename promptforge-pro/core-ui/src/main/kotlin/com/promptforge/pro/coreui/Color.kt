package com.promptforge.pro.coreui

import androidx.compose.ui.graphics.Color

/**
 * Palette identica ai token CSS di PromptForge_Pro_v7.1_Guided_Studio.html
 * (l'app HTML che l'utente usa davvero) — non una reinterpretazione: stessi
 * valori esadecimali, stesso ruolo di ciascun colore (viola = azione
 * primaria, verde = soggetto/identità, arancione = camera, rosa = rollio/
 * modalità adulti, ciano/giallo = luce).
 */
object PromptForgeColors {
    val Background = Color(0xFF0A0A0C)
    val Surface = Color(0xFF121214)
    val Card = Color(0xFF18181B)
    val Card2 = Color(0xFF1E1E22)
    val Card3 = Color(0xFF25252A)

    val Border = Color(0xFF232327)
    val Border2 = Color(0xFF2E2E35)
    val Border3 = Color(0xFF3A3A42)

    val Text = Color(0xFFF0F0F2)
    val Muted = Color(0xFF555555)
    val Muted2 = Color(0xFF888888)
    val Muted3 = Color(0xFFAAAAAA)

    val Violet = Color(0xFF7C6AF7)
    val VioletDark = Color(0xFF5B4FD4)
    val VioletLight = Color(0xFFA89CF7)
    val VioletBg = Color(0x147C6AF7)

    val Green = Color(0xFF2ED58A)
    val Yellow = Color(0xFFF5A623)
    val Red = Color(0xFFE85555)
    val Pink = Color(0xFFE05F9A)
    val Cyan = Color(0xFF5BC0DE)
    val Orange = Color(0xFFFF8C42)

    val Error = Red
}
