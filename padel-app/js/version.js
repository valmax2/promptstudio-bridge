// Sostituito con il numero di build reale (es. "V53") dal workflow CI prima
// della compilazione (vedi .github/workflows/build-padel-apk.yml) - "dev"
// resta solo quando si apre index.html direttamente senza passare da una
// build, cioè durante lo sviluppo/anteprima locale.
export const APP_VERSION = 'dev';
